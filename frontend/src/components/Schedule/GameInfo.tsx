import React from "react";

type GameProps = {
    home_team: string,
    home_team_id: number,
    home_team_score: number,
    away_team: string,
    away_team_id: number,
    away_team_score: number,
    location: string,
    city: string,
    date: string,
    time_left: string
}

const GameInfo = (props: GameProps) => {

    return (
        // full content div
        <div>

            {/*  */}

        </div>
    )
}

export default GameInfo